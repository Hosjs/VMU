import { useLoaderData } from "react-router";
import type { Route } from "./+types/roles";

export default function RolesPage() {
  return (
    <div>
      <h1>Quản lý vai trò & quyền</h1>
      {/* Component sẽ được migrate từ admin/roles.tsx */}
    </div>
  );
}

