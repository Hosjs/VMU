import { useLoaderData } from "react-router";
import type { Route } from "./+types/products";

export default function ProductsPage() {
  return (
    <div>
      <h1>Quản lý sản phẩm</h1>
      {/* Component sẽ được migrate từ admin/products.tsx */}
    </div>
  );
}

