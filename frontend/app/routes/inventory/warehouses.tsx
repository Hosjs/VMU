import { useLoaderData } from "react-router";
import type { Route } from "./+types/warehouses";

export default function WarehousesPage() {
  return (
    <div>
      <h1>Quản lý kho</h1>
      {/* Component sẽ được migrate từ admin/warehouses.tsx */}
    </div>
  );
}

