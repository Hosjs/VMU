import { useLoaderData } from "react-router";
import type { Route } from "./+types/dashboard";

export default function ReportsDashboardPage() {
  return (
    <div>
      <h1>Dashboard & Báo cáo</h1>
      {/* Component sẽ được migrate từ admin/reports.tsx */}
    </div>
  );
}
