import { useLoaderData } from "react-router";
import type { Route } from "./+types/providers";

export default function ProvidersPage() {
  return (
    <div>
      <h1>Quản lý nhà cung cấp</h1>
      {/* Component sẽ được migrate từ admin/providers.tsx */}
    </div>
  );
}

