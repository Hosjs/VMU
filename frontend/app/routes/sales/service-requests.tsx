import { useLoaderData } from "react-router";
import type { Route } from "./+types/service-requests";

export default function ServiceRequestsPage() {
  return (
    <div>
      <h1>Yêu cầu dịch vụ</h1>
      {/* Component sẽ được migrate từ admin/service-requests.tsx */}
    </div>
  );
}

