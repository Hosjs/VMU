import { useLoaderData } from "react-router";
import type { Route } from "./+types/orders";

export default function OrdersPage() {
  return (
    <div>
      <h1>Quản lý đơn hàng</h1>
      {/* Component sẽ được migrate từ admin/orders.tsx */}
    </div>
  );
}

