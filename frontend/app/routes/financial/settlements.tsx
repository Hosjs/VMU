import { useLoaderData } from "react-router";
import type { Route } from "./+types/settlements";

export default function SettlementsPage() {
  return (
    <div>
      <h1>Quản lý quyết toán</h1>
      {/* Component sẽ được migrate từ admin/settlements.tsx */}
    </div>
  );
}

