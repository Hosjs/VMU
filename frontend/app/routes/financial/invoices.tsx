import { useLoaderData } from "react-router";
import type { Route } from "./+types/invoices";

export default function InvoicesPage() {
  return (
    <div>
      <h1>Quản lý hóa đơn</h1>
      {/* Component sẽ được migrate từ admin/invoices.tsx */}
    </div>
  );
}

