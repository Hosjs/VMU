import { useLoaderData } from "react-router";
import type { Route } from "./+types/payments";

export default function PaymentsPage() {
  return (
    <div>
      <h1>Quản lý thanh toán</h1>
      {/* Component sẽ được migrate từ admin/payments.tsx */}
    </div>
  );
}

